import fs from 'node:fs/promises';
const build=JSON.parse(await fs.readFile('reports/build-summary.json','utf8'));
const valid=JSON.parse(await fs.readFile('reports/validation.json','utf8'));
const browser=JSON.parse(await fs.readFile('reports/browser-validation.json','utf8'));
const similarity=JSON.parse(await fs.readFile('reports/similarity.json','utf8'));
const pages=JSON.parse(await fs.readFile('content/pages.json','utf8'));
const files=await fs.readdir('assets/images');let originalBytes=0;
for(const name of files)originalBytes+=(await fs.stat('assets/images/'+name)).size;
const derivatives=await fs.readdir('dist/assets/images');let jpegBytes=0,webpBytes=0;
for(const stem of [...new Set(derivatives.map(f=>f.replace(/-\d+\.(jpg|webp)$/,'')))]){
 for(const ext of ['jpg','webp']){
  const biggest=derivatives.filter(f=>f.startsWith(stem+'-')&&f.endsWith('.'+ext)).sort((a,b)=>Number(b.match(/-(\d+)\./)[1])-Number(a.match(/-(\d+)\./)[1]))[0];
  const size=(await fs.stat('dist/assets/images/'+biggest)).size;if(ext==='jpg')jpegBytes+=size;else webpBytes+=size;
 }
}
const report=`# Archive.London build and checks\n\n## Current result\n\n${build.pages} pages built from ${build.suppliedPages} supplied content files and three supporting pages. Every supplied file has a matching route. The site is a local review build; the live domain has not been switched.\n\n- ${build.collectionPages} collection pages, including ${build.byType.principal_collection} principal collections, two place collections and two format collections.\n- No standalone brand pages, individual-person collection pages or individual-event pages have been invented. The biography is a separate ProfilePage.\n- ${build.pagesWithImages} page with existing images: the homepage. All ${build.unillustratedCollectionPages} collection pages are intentionally unillustrated pending the final photograph batch.\n- Ten existing photographs retained. WebP and JPEG derivatives, intrinsic dimensions, responsive srcsets, visible credits and image-specific alt text are included.\n- Original image bytes: ${originalBytes.toLocaleString('en-GB')}. Largest WebP derivative per photograph totals ${webpBytes.toLocaleString('en-GB')} bytes; largest JPEG fallback per photograph totals ${jpegBytes.toLocaleString('en-GB')} bytes. These are alternative format totals, not a page-load measurement.\n- File 40 and Batch 25 (230–239) were not supplied and are not assumed.\n\n## Design system\n\nIvory and warm neutrals dominate, with charcoal text and restrained heritage-green accents. Self-hosted Cormorant Garamond and Inter use regular and medium weights. Gold is limited to wordmark and favicon details. Spacious layouts, unframed photographs and text-only collection headings replace decorative initials. Fonts are compressed as WOFF2 with their licences included. Fixed ivory navigation, a reserved scrolling region and minimal green footers apply site-wide. Photographs are centred and fitted to the available viewport; narrative and filters remain expanded. The homepage year strip is separate from all images.\n\n## Validation\n\n- Static validation: ${valid.passed?'passed':'failed'} across ${valid.routeCount} routes.\n- Broken internal links, orphan pages, duplicate titles, duplicate descriptions and duplicate H1s: zero.\n- Sitemap: every indexable page included, no filter combinations.\n- Canonicals and sharing metadata: page-specific. Unillustrated pages do not receive unrelated sharing images.\n- Schema: JSON syntax, expected entity types, references, visible item lists and displayed images checked locally. This is not an external search-engine certification.\n- Editorial similarity: highest five-word Jaccard score ${(similarity.topPairs[0].jaccardFiveWord*100).toFixed(2)}%, between ${similarity.topPairs[0].a} and ${similarity.topPairs[0].b}. Repeated long paragraphs: ${similarity.duplicatedParagraphs.length}.\n- Browser verification: ${browser.passed?'passed':'failed'} on ${browser.pagesTested} pages. No reported script errors. Desktop automated axe WCAG A/AA checks passed; 320px and 390px overflow checks passed. Search, empty results, reset, brand/format filtering, query restoration, no-JavaScript navigation, contact prefill, 404 response and reduced motion were exercised.\n- Performance: no third-party scripts or font requests; images are responsive and compressed. Local page timings are recorded in the detailed browser report. Production Core Web Vitals and full manual accessibility conformance remain to be assessed on the deployed site.\n\n## Content handling\n\nPublic copy retains the supplied substantive editorial content. Front matter is used as metadata; production notes are converted to reader-facing wording or held out of the page. Source headings are normalised into one H1 and a logical heading sequence. The exact changes are recorded in editorial-changes.json.\n\nSupplied counts are preserved as owner-provided catalogue assertions. No inventory rows have been reconciled or consolidated because the underlying spreadsheet is not yet available. Category overlap is preserved and totals are not added together. Watch associations are described as catalogue associations rather than a claim of personal ownership.\n\n## Remaining work\n\n- Receive, review, compress and allocate the final photographs.\n- Reconcile subject counts, dates and object associations with the underlying inventory.\n- Add further supplied content, including file 40 if intended.\n- Confirm hosting, domain/DNS/HTTPS, old-domain 301 redirects and contact details. No server-side domain redirects have been activated. Four legacy homepage anchor destinations are handled in the new site.\n- Verify the published site and sitemap after launch.\n\n## Referenced pages awaiting content\n\nThese paths were mentioned in supplied files but do not have their own content in this batch. They are withheld, recorded for later and not left as broken collection links. In-body calls to action use subject-specific enquiry links.\n\n${build.unavailableLinkedRoutes.map(p=>'- '+p).join('\n')}\n\n## Page coverage\n\n| Supplied file | Built route |\n| --- | --- |\n${pages.map(p=>`| ${p.sourceFile} | ${p.path} |`).join('\n')}\n\nTechnical references: [Schema.org CollectionPage](https://schema.org/CollectionPage) and [Google Search Central canonical guidance](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls).\n`;
await fs.writeFile('reports/build-report.md',report);
console.log('Wrote reports/build-report.md');

